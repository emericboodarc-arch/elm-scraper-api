services:
  - type: web
    name: elm-scraper-api
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn app:app --workers 1 --timeout 3600 --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: /opt/render/project/.browsers
